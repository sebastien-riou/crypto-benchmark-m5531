#!/bin/bash

set -ex

echo "Does not work! use debug"
#pyocd gdbserver --port 3333 --probe cmsisdap: --connect attach --persist --reset-run --cbuild-run make.cbuild-run.yml

#pyocd list --cbuild-run make.cbuild-run.yml