#!/bin/bash

pyocd load --probe cmsisdap: --cbuild-run make.cbuild-run.yml

if [ $? -eq 0 ] ; then
    echo "Done"
else
    #try a second time
    echo "Retry in 5 second"
    sleep 5
    pyocd load --probe cmsisdap: --cbuild-run make.cbuild-run.yml
fi